import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Angular-FK13',
  webDir: 'dist/angular-fk13/browser'
};

export default config;
